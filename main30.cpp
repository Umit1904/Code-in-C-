//30
#include <bits/stdc++.h>
using namespace std;
int main (){
    int a;
    cin>>a;
    cout<<pow(a,2)*pow(a,2)<<endl;
    cout<<pow(a,2)*pow(a,2)*pow(a,2)<<endl;
    cout<<pow(a,3)*pow(a,3)*pow(a,3)*pow(a,3)*pow(a,3);
    return 0;
}


